{
  "name": "online-judge-platform",
  "version": "1.0.0",
  "description": "Scalable Online Judge Platform with Docker Sandboxing and BullMQ Async Queues",
  "author": "Kuswanth Tumma",
  "license": "MIT",
  "scripts": {
    "dev:backend": "cd backend && npm run dev",
    "dev:worker": "cd worker && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build --prefix backend && npm run build --prefix worker",
    "docker:up": "docker-compose up --build -d",
    "docker:down": "docker-compose down"
  }
}
